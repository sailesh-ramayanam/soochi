# Description
A `Counter` is a simple application that displays a number and allows the user to
1. Increment or
2. Decrement or
3. Reset

Please refer to `Sample UI` section below, for an example.

In this assignment you will have to implement a `Counter` application using `React`.

# Requirements

## UI requirements

1. Title of the increment button should be `Increment`.
2. Title of the decrement button should be `Decrement`.
3. Title of the reset button should be `Reset`.
4. Initially value of the counter should be `0`.

## Functional requirements

1. On clicking the `Increment` button, counter value must increase by 1.
2. On clicking the `Decrement`, button counter value must decrease by 1.
3. On clicking the `Reset` button, counter value must reset to 0.
4. Minimum value for counter should be 0 (i.e. when you click decrement button when the counter is 0, it should remain as 0).

# Sample UI

[Click here](https://drive.google.com/file/d/1_rOcO85pg4cfPz9dWdMUd1Zo0tsIa9EN/view?usp=sharing) for the image of a sample UI

# Optional Challenge
Implement the same `Counter` application using `HTML/CSS/JS`. You don't need to submit it.
1. Compare it with the `React` implementation.
2. Note down the aspects which are made easy by `React`.